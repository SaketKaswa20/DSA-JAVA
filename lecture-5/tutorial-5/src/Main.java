public class Main {
    public static void main(String[] args) {
        //Arrays
        //Syntax:
        //datatype[] variable_name=new datatype[size];

        //Example: Store 5 roll numbers
        // int[] roll_no= new int[5];
        //or directly write the roll numbers as shown below
        // int[] roll_no= {23,15,24,56,78};

        //All the type of data in the Array should be same

//        int[] rolls= new int[5];
//        System.out.println(rolls[1]); //By default for a array of strings unless stored anything the answer is zero

        String[] name= new String[5];
        System.out.println(name[2]); //By default for a array of string unless stored anything the answer is null
    }
}